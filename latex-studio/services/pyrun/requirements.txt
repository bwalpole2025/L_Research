# Scientific stack for the Python "Run" sandbox (pinned, verified on Python 3.12).
# Kept deliberately small: numerics + plotting only. matplotlib uses the Agg
# backend (MPLBACKEND=Agg) so it never opens a window.
numpy==2.1.3
scipy==1.14.1
matplotlib==3.9.2

# Web framework
fastapi>=0.111,<0.116
uvicorn[standard]>=0.30
pydantic>=2.7
httpx>=0.27

# Math stack (versions verified to interoperate on Python 3.12):
#  - latex2sympy2 1.9.1 pins antlr4-python3-runtime to 4.7.2 (its generated parser),
#    so SymPy's antlr LaTeX backend is unusable — we use the Lark backend instead.
sympy==1.14.0
latex2sympy2==1.9.1
antlr4-python3-runtime==4.7.2
lark>=1.1

# Tests
pytest>=8.2

# Review-PDF annotation (AGPL-3.0; OK for single-user local use — ADR-009)
pymupdf>=1.24

# Local sentence embeddings for the RAG document check. Offline after a one-time
# model-weights download from Hugging Face on first use (see README runbook); the
# model (BAAI/bge-small-en-v1.5, 384-dim) is then cached on disk and nothing
# leaves the machine. Pulls in a CPU build of torch.
sentence-transformers>=3.0
torch>=2.2
